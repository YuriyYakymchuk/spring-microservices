spring:
  application:
    name: Turbine
  cloud:
    config:
      uri: http://localhost:9000/
      fail-fast: true
      retry:
        initial-interval: 2000
        max-attempts: 3
        max-attempts: 3